# 爆破模式·计划A（通用战士层）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把"只会打玩家的 6 只红蛋"升级成"蓝队(你+蛋定/蛋壳/蛋黄/蛋花) vs 红队(皮蛋/咸蛋/铁蛋/坏蛋/笨蛋)互射的通用战士层"，交付一个可玩的 5v5 混战（一队全灭→横幅→自动重开），为计划B（回合/爆蛋器）打地基。

**Architecture:** 新增纯逻辑 `Roster` 战斗员名册（位置/速度/血量/存活/队伍）；感知升级为多目标挑选（最近可见敌）；瞄准支持按枪配置节奏；大脑输出 `knife` 意图（切刀加速）；`eggBots.ts` 增加 match 模式（两队、真枪伤害 dmgAt、死亡事件、不重生、回合重置 API），practice 模式行为保持现状；main.ts 起 5v5 混战；小地图画队友。

**Tech Stack:** TypeScript 严格模式、Three.js（仅身体层）、esbuild 测试（`npm run test:fast`）。

## Global Constraints

- 注释、测试描述一律简体中文；`src/game/ai/`、`src/game/combat/` 纯逻辑模块**禁止 import 'three'**。
- 测试断言 shim 支持 `toBe`/`toEqual`/`toBeCloseTo`；没有 beforeEach；新测试文件必须在 `scripts/run-logic-tests.ts` 注册。
- 测试跑 `npm run test:fast`（不要 `npm test`）；判断"全绿"看**失败数=0**（总数会因并行会话漂移）；类型检查 `npm run build`。
- **practice（训练场）模式行为不变**：6 只红蛋、速度 SPEED=2.6、死后 3 秒重生、只打玩家。
- match 模式蛋速：刀 6.75 ｜ 枪 = `gun.runSpeed`；蛋伤害 = `dmgAt(gun, 距离, false)` 永不爆头；枪声半径 = `suppressed?10:30`；脚步 8（沿用 SENSE）。
- 友军火力关闭：玩家(蓝)打不伤蓝蛋，蛋只选敌队目标。
- 碰撞半径沿用现状：strafe blocked 0.7、pushOut 0.72。
- 蛋名字固定：蓝=蛋定/蛋壳/蛋黄/蛋花，红=皮蛋/咸蛋/铁蛋/坏蛋/笨蛋；玩家 id='player' name='你' team='blue'。
- **并行会话协调**：绝不改动/提交 `desertMap.ts/townProps.ts/penetration.ts/mapGrid.ts/mapData.ts` 等地图与穿透文件；每任务只提交自己声明的文件；开工先看 `git log -3` 了解漂移。
- git 提交信息中文 `feat(爆破A): …` 风格 + 结尾 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 高台/天台限制（已知，不修）：AI 是 2D 的，不会上台阶、不理解高度；v1 接受。

---

### Task 1: Roster 战斗员名册（纯逻辑）

**Files:**
- Create: `src/game/combat/roster.ts`
- Test: `tests/roster.test.ts`
- Modify: `scripts/run-logic-tests.ts`（加 `import '../tests/roster.test';`）

**Interfaces:**
- Produces（后续任务依赖，逐字匹配）:
  - `type TeamId = 'blue' | 'red'`
  - `interface Combatant { id: string; name: string; team: TeamId; x: number; z: number; vx: number; vz: number; hp: number; alive: boolean; weaponId: string; isPlayer: boolean }`
  - `class Roster { add(c): Combatant; get(id): Combatant|undefined; all(): readonly Combatant[]; aliveEnemiesOf(team): Combatant[]; aliveCount(team): number; updatePos(id,x,z,dt): void; setAlive(id,alive): void; setHp(id,hp): void; setWeapon(id,weaponId): void }`
  - `add` 入参为 `Omit<Combatant,'vx'|'vz'>`（速度从 0 起由 updatePos 推导，瞬移>20m/s 记 0）

- [ ] **Step 1: 写失败测试** `tests/roster.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { Roster } from '../src/game/combat/roster';

const mk = (id: string, team: 'blue' | 'red', alive = true) =>
  ({ id, name: id, team, x: 0, z: 0, hp: 100, alive, weaponId: 'classic', isPlayer: id === 'player' });

describe('Roster 名册', () => {
  it('登记后能按 id 取到', () => {
    const r = new Roster();
    r.add(mk('player', 'blue'));
    expect(r.get('player')!.name).toBe('player');
    expect(r.all().length).toBe(1);
  });
  it('aliveEnemiesOf 只给活着的敌队', () => {
    const r = new Roster();
    r.add(mk('player', 'blue'));
    r.add(mk('蛋定', 'blue'));
    r.add(mk('皮蛋', 'red'));
    r.add(mk('咸蛋', 'red', false)); // 死的不算
    const es = r.aliveEnemiesOf('blue');
    expect(es.length).toBe(1);
    expect(es[0].id).toBe('皮蛋');
    expect(r.aliveEnemiesOf('red').length).toBe(2);
  });
  it('aliveCount 数活人', () => {
    const r = new Roster();
    r.add(mk('a', 'blue')); r.add(mk('b', 'blue', false)); r.add(mk('c', 'red'));
    expect(r.aliveCount('blue')).toBe(1);
    r.setAlive('a', false);
    expect(r.aliveCount('blue')).toBe(0);
  });
  it('updatePos 推导速度,瞬移不算', () => {
    const r = new Roster();
    r.add(mk('a', 'red'));
    r.updatePos('a', 1, 0, 0.1); // 第一次:从(0,0)到(1,0),0.1秒 → vx=10
    expect(r.get('a')!.vx).toBeCloseTo(10, 5);
    r.updatePos('a', 100, 0, 0.1); // 瞬移(990m/s>20) → 记0
    expect(r.get('a')!.vx).toBe(0);
    expect(r.get('a')!.x).toBe(100); // 位置照更新
  });
  it('setHp/setWeapon 生效', () => {
    const r = new Roster();
    r.add(mk('a', 'red'));
    r.setHp('a', 55); r.setWeapon('a', 'ghost');
    expect(r.get('a')!.hp).toBe(55);
    expect(r.get('a')!.weaponId).toBe('ghost');
  });
});
```

- [ ] **Step 2: 注册并跑，确认失败**

`scripts/run-logic-tests.ts` import 区加一行 `import '../tests/roster.test';`
Run: `npm run test:fast` → Expected: `Could not resolve "../src/game/combat/roster"`

- [ ] **Step 3: 实现** `src/game/combat/roster.ts`

```ts
// 战斗员名册：两队所有人(含玩家)的位置/速度/血量/存活/手持武器。
// 纯逻辑：蛋的感知从这里拿"敌人有谁"，瞄准从这里拿目标速度。
export type TeamId = 'blue' | 'red';

export interface Combatant {
  id: string; name: string; team: TeamId;
  x: number; z: number;
  vx: number; vz: number;   // 由 updatePos 推导(瞄准甩枪用)
  hp: number; alive: boolean;
  weaponId: string;         // 手持枪 id('classic'...)
  isPlayer: boolean;
}

const TELEPORT_SPEED = 20; // 超过这个速度视为瞬移(重生/传送),速度记0

export class Roster {
  private list: Combatant[] = [];
  private byId = new Map<string, Combatant>();

  add(c: Omit<Combatant, 'vx' | 'vz'>): Combatant {
    const full: Combatant = { ...c, vx: 0, vz: 0 };
    this.list.push(full);
    this.byId.set(full.id, full);
    return full;
  }

  get(id: string): Combatant | undefined { return this.byId.get(id); }
  all(): readonly Combatant[] { return this.list; }

  aliveEnemiesOf(team: TeamId): Combatant[] {
    return this.list.filter((c) => c.alive && c.team !== team);
  }
  aliveCount(team: TeamId): number {
    return this.list.reduce((n, c) => n + (c.team === team && c.alive ? 1 : 0), 0);
  }

  updatePos(id: string, x: number, z: number, dt: number): void {
    const c = this.byId.get(id);
    if (!c) return;
    if (dt > 1e-4) {
      const vx = (x - c.x) / dt, vz = (z - c.z) / dt;
      const sp = Math.hypot(vx, vz);
      if (sp > TELEPORT_SPEED) { c.vx = 0; c.vz = 0; } else { c.vx = vx; c.vz = vz; }
    }
    c.x = x; c.z = z;
  }

  setAlive(id: string, alive: boolean): void { const c = this.byId.get(id); if (c) c.alive = alive; }
  setHp(id: string, hp: number): void { const c = this.byId.get(id); if (c) c.hp = hp; }
  setWeapon(id: string, weaponId: string): void { const c = this.byId.get(id); if (c) c.weaponId = weaponId; }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test:fast` → Expected: 失败数 0

- [ ] **Step 5: 提交**

```bash
git add src/game/combat/roster.ts tests/roster.test.ts scripts/run-logic-tests.ts
git commit -m "feat(爆破A): 战斗员名册Roster——两队成员/速度推导/存活统计

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 感知多目标——挑"最近可见敌"

**Files:**
- Modify: `src/game/ai/botSenses.ts`
- Test: `tests/botSenses.test.ts`（追加一个 describe）

**Interfaces:**
- Consumes: 现有 `BotSenses`（visible/lastKnown/heard/losClear）
- Produces:
  - `interface TargetPing { id: string; x: number; z: number }`
  - `BotSenses` 新增字段 `visibleId: string | null`
  - `updateVisionMulti(bx, bz, faceX, faceZ, cands: TargetPing[], solids: Box[]): void` —— 候选按距离从近到远，第一个过"距离/扇形/视线"三关的成为可见目标（设 visible/visibleId/lastKnown、清 heard）；都不过则 visible=false, visibleId=null
  - 旧 `updateVision(bx,bz,fx,fz,px,pz,playerAlive,solids)` 改为薄封装：`updateVisionMulti(bx,bz,fx,fz, playerAlive?[{id:'player',x:px,z:pz}]:[], solids)`（旧行为与旧测试不变）
  - `reset()` 同时清 visibleId

- [ ] **Step 1: 追加失败测试**（`tests/botSenses.test.ts` 末尾新增）

```ts
describe('BotSenses 多目标挑选', () => {
  // 注意墙要够厚(z向2米)：losClear 每1.4米采样一次,太薄会被采样跨过去摸不到
  const wall2: Box = { min: { x: -0.5, y: 0, z: 2 }, max: { x: 0.5, y: 2, z: 4 } };
  it('挑最近的可见敌人', () => {
    const s = new BotSenses();
    s.updateVisionMulti(0, 0, 0, 1, [
      { id: '远', x: 0, z: 15 },
      { id: '近', x: 0, z: 8 },
    ], []);
    expect(s.visible).toBe(true);
    expect(s.visibleId).toBe('近');
    expect(s.lastKnown!.z).toBe(8);
  });
  it('最近的被墙挡住 → 挑更远但看得见的', () => {
    const s = new BotSenses();
    s.updateVisionMulti(0, 0, 0, 1, [
      { id: '被挡', x: 0, z: 8 },        // 视线穿 wall2(z∈[2,4]) 被挡
      { id: '侧面', x: 10, z: 10 },      // 斜前方,不穿墙
    ], [wall2]);
    expect(s.visible).toBe(true);
    expect(s.visibleId).toBe('侧面');
  });
  it('全部不可见 → visible=false 且 visibleId=null', () => {
    const s = new BotSenses();
    s.updateVisionMulti(0, 0, 0, 1, [{ id: 'a', x: 0, z: -10 }], []); // 在正后方
    expect(s.visible).toBe(false);
    expect(s.visibleId === null).toBe(true);
  });
  it('reset 清掉 visibleId', () => {
    const s = new BotSenses();
    s.updateVisionMulti(0, 0, 0, 1, [{ id: 'a', x: 0, z: 5 }], []);
    s.reset();
    expect(s.visibleId === null).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:fast` → Expected: TS/打包报错 `updateVisionMulti` 不存在（或运行失败）

- [ ] **Step 3: 实现**（改 `src/game/ai/botSenses.ts`）

在 `Known` 接口后加：

```ts
export interface TargetPing { id: string; x: number; z: number }
```

`BotSenses` 类：`visible = false;` 之后加一行字段：

```ts
  visibleId: string | null = null; // 当前看见的是谁(敌人id)
```

把现有 `updateVision` 整个替换为：

```ts
  // 多目标视野：候选按距离从近到远，第一个过"距离/扇形/视线"三关的成为可见目标
  updateVisionMulti(bx: number, bz: number, faceX: number, faceZ: number, cands: TargetPing[], solids: Box[]): void {
    this.visible = false;
    this.visibleId = null;
    const fl = Math.hypot(faceX, faceZ);
    const sorted = cands
      .map((c) => ({ c, d: Math.hypot(c.x - bx, c.z - bz) }))
      .sort((a, b) => a.d - b.d);
    for (const { c, d } of sorted) {
      if (d > SENSE.VIEW_DIST || d < 1e-3) continue;
      if (fl > 1e-6 && (faceX * (c.x - bx) + faceZ * (c.z - bz)) / (fl * d) < COS_HALF_FOV) continue; // 扇形外
      if (!losClear(bx, bz, c.x, c.z, solids)) continue; // 被墙挡
      this.visible = true;
      this.visibleId = c.id;
      this.lastKnown = { x: c.x, z: c.z, age: 0 };
      this.heard = null; // 都看见了，疑点作废
      return;
    }
  }

  // 旧接口(单目标=玩家)：训练场沿用
  updateVision(bx: number, bz: number, faceX: number, faceZ: number, px: number, pz: number, playerAlive: boolean, solids: Box[]): void {
    this.updateVisionMulti(bx, bz, faceX, faceZ, playerAlive ? [{ id: 'player', x: px, z: pz }] : [], solids);
  }
```

`reset()` 改为：

```ts
  reset(): void { this.visible = false; this.visibleId = null; this.lastKnown = null; this.heard = null; }
```

- [ ] **Step 4: 跑测试确认通过**（旧的单目标测试也必须原样全过）

Run: `npm run test:fast` → Expected: 失败数 0；`npm run build` → 零错误

- [ ] **Step 5: 提交**

```bash
git add src/game/ai/botSenses.ts tests/botSenses.test.ts
git commit -m "feat(爆破A): 感知多目标——从候选敌人里挑最近可见者(旧单目标接口保持)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 瞄准按枪配节奏

**Files:**
- Modify: `src/game/ai/botAim.ts`
- Test: `tests/botAim.test.ts`（追加一个 describe）

**Interfaces:**
- Produces:
  - `interface AimCfg { shotGap: number; burstMin: number; burstMax: number; burstGap: number }`
  - `BotAim` 新增 `cfg: AimCfg`（默认=AIM 常量值）与 `setCfg(c: AimCfg): void`；`update` 内点射节奏改用 `this.cfg`；`reset` **不**还原 cfg（武器跨回合保留）
  - `aimCfgForGun(fireCd: number, auto: boolean): AimCfg` —— `gap=max(fireCd,0.12)`；自动枪 `{shotGap:gap, burstMin:3, burstMax:5, burstGap:0.7}`；半自动 `{shotGap:max(gap,0.25), burstMin:1, burstMax:2, burstGap:0.8}`

- [ ] **Step 1: 追加失败测试**（`tests/botAim.test.ts` 末尾）

```ts
describe('BotAim 按枪配节奏', () => {
  it('aimCfgForGun：自动枪射速快点射长', () => {
    const c = aimCfgForGun(0.1, true); // 狂怒 fireCd=0.1
    expect(c.shotGap).toBeCloseTo(0.12, 5); // 不快于0.12
    expect(c.burstMin).toBe(3);
    expect(c.burstMax).toBe(5);
  });
  it('aimCfgForGun：半自动慢而稳', () => {
    const c = aimCfgForGun(0.25, false); // 正义 fireCd=0.25
    expect(c.shotGap).toBeCloseTo(0.25, 5);
    expect(c.burstMin).toBe(1);
    expect(c.burstMax).toBe(2);
  });
  it('setCfg 改变实际开火间隔', () => {
    const aim = new BotAim(); aim.reset(0, 10);
    aim.setCfg({ shotGap: 0.5, burstMin: 2, burstMax: 2, burstGap: 2 });
    const shots = run(aim, 3, { x: 0, z: 10 });
    const gaps = shots.slice(1).map((e, i) => e.t - shots[i].t);
    expect(gaps.every((g) => g > 0.45)).toBe(true); // 全部≥0.5左右,没有0.22的默认快间隔
  });
  it('reset 不清掉 cfg(武器保留)', () => {
    const aim = new BotAim();
    aim.setCfg({ shotGap: 0.5, burstMin: 2, burstMax: 2, burstGap: 2 });
    aim.reset(0, 0);
    expect(aim.cfg.shotGap).toBe(0.5);
  });
});
```

同时把该文件顶部 import 改为：`import { BotAim, AIM, aimCfgForGun } from '../src/game/ai/botAim';`

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:fast` → Expected: `aimCfgForGun` 不存在的解析/类型错误

- [ ] **Step 3: 实现**（改 `src/game/ai/botAim.ts`）

`Shot` 接口后加：

```ts
export interface AimCfg { shotGap: number; burstMin: number; burstMax: number; burstGap: number }

// 按枪映射点射节奏：射速快的自动枪→短间隔长点射；半自动→单发/双发慢点
export function aimCfgForGun(fireCd: number, auto: boolean): AimCfg {
  const gap = Math.max(fireCd, 0.12);
  return auto
    ? { shotGap: gap, burstMin: 3, burstMax: 5, burstGap: 0.7 }
    : { shotGap: Math.max(gap, 0.25), burstMin: 1, burstMax: 2, burstGap: 0.8 };
}
```

`BotAim` 类内加字段与方法（`aimX = 0; aimZ = 0;` 之后）：

```ts
  cfg: AimCfg = { shotGap: AIM.SHOT_GAP, burstMin: AIM.BURST_MIN, burstMax: AIM.BURST_MAX, burstGap: AIM.BURST_GAP };
  setCfg(c: AimCfg): void { this.cfg = c; }
```

`update` 内四处替换：`AIM.BURST_MIN + Math.floor(rng() * (AIM.BURST_MAX - AIM.BURST_MIN + 1))` → `this.cfg.burstMin + Math.floor(rng() * (this.cfg.burstMax - this.cfg.burstMin + 1))`；`this.shotT = this.burstLeft > 0 ? AIM.SHOT_GAP : AIM.BURST_GAP;` → `this.shotT = this.burstLeft > 0 ? this.cfg.shotGap : this.cfg.burstGap;`

- [ ] **Step 4: 跑测试确认通过**（旧节奏测试用默认 cfg，必须原样全过）

Run: `npm run test:fast` → Expected: 失败数 0

- [ ] **Step 5: 提交**

```bash
git add src/game/ai/botAim.ts tests/botAim.test.ts
git commit -m "feat(爆破A): 瞄准按枪配节奏——自动枪长点射/半自动慢单发/reset保留武器配置

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 大脑吃"敌人列表"+ 切刀意图

**Files:**
- Modify: `src/game/ai/botBrain.ts`、`tests/botBrain.test.ts`（同任务内迁移签名）

**Interfaces:**
- Consumes: Task 2 的 `TargetPing`、`updateVisionMulti`、`visibleId`
- Produces（Task 5 依赖，逐字匹配）:
  - `Drive` 新增字段 `knife: boolean`（身体据此定速度；初始 false）
  - `think` 第三参从 `player: {x,z,alive}` 改为 `targets: TargetPing[]`（活着的敌人；空数组=无敌人/玩家阵亡）
  - `BRAIN` 新增 `KNIFE_DIST: 8`
  - 切刀规则：`knife = 没有任何威胁(visible/lastKnown/heard/squad.shared全空) 且 当前路径目的地距离>8m`；其余情况 false
  - 战斗焦点：可见时用 `targets` 里 `visibleId` 那个的位置；否则沿用记忆/共享情报

- [ ] **Step 1: 迁移并追加测试**（`tests/botBrain.test.ts`）

顶部辅助区把 `noSquad` 之后加：

```ts
const T = (id: string, x: number, z: number) => ({ id, x, z });
```

全文机械替换（12 个既有用例）：`{ x: A, z: B, alive: true }` 形式的第三参 → `[T('p', A, B)]`；唯一的 `alive: false` 场景（若有）与"玩家阵亡"语义 → `[]`。例如：
`b.think(openWorld(), self0(), { x: 0, z: 8, alive: true }, noSquad, 0.1, true, r0);` → `b.think(openWorld(), self0(), [T('p', 0, 8)], noSquad, 0.1, true, r0);`

文件末尾追加：

```ts
describe('BotBrain 切刀加速', () => {
  it('没威胁+目的地远 → 亮刀赶路', () => {
    const b = new BotBrain(); b.reset(r0);
    b.think(openWorld(), self0(), [], noSquad, 0.1, true, r0); // 无敌人→patrol,目的地(30,30)很远
    expect(b.drive.mode).toBe('patrol');
    expect(b.drive.knife).toBe(true);
  });
  it('听到动静 → 收刀掏枪', () => {
    const b = new BotBrain(); b.reset(r0);
    b.senses.hearAt(6, 6, r0);
    b.think(openWorld(), self0(), [T('p', 0, 40)], noSquad, 0.1, true, r0);
    expect(b.drive.knife).toBe(false);
  });
  it('看见敌人 → 收刀', () => {
    const b = new BotBrain(); b.reset(r0);
    b.think(openWorld(), self0(), [T('p', 0, 8)], noSquad, 0.1, true, r0);
    expect(b.drive.mode).toBe('combat');
    expect(b.drive.knife).toBe(false);
  });
  it('多个敌人时交战对象是最近可见者', () => {
    const b = new BotBrain(); b.reset(r0);
    b.think(openWorld(), self0(), [T('远', 0, 20), T('近', 0, 6)], noSquad, 0.1, true, r0);
    expect(b.drive.mode).toBe('combat');
    expect(b.senses.visibleId).toBe('近');
    // 面朝最近者
    expect(b.drive.face!.z > 0).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:fast` → Expected: think 参数类型不匹配的编译错

- [ ] **Step 3: 实现**（改 `src/game/ai/botBrain.ts`）

- import 行把 `BotSenses, Known` 扩成 `BotSenses, Known, TargetPing`（from './botSenses'）。
- `BRAIN` 常量对象末尾加：`KNIFE_DIST: 8, // 距目的地大于这个才切刀赶路`
- `Drive` 接口加：`knife: boolean; // 亮刀赶路(速度=刀速),身体据此定速`
- `reset()` 与类字段里 drive 初始字面量补 `knife: false`（两处）。
- `think` 签名：`player: { x: number; z: number; alive: boolean }` → `targets: TargetPing[]`。
- think 顶部 `d.strafe = null; ...` 行补 `d.knife = false;`。
- 准备阶段分支 `if (!combatOn || !player.alive)` → `if (!combatOn)`（玩家阵亡由空 targets 表达；无目标仍可巡逻）。分支内在 `this.patrol(w, self);` 之后、`return` 之前插入 `this.updateKnife(self);`。
- 视野行替换：`this.senses.updateVision(self.x, self.z, self.faceX, self.faceZ, player.x, player.z, true, w.solids);` → `this.senses.updateVisionMulti(self.x, self.z, self.faceX, self.faceZ, targets, w.solids);`
- `tp` 计算替换：

```ts
    const vis = this.senses.visible ? targets.find((t) => t.id === this.senses.visibleId) ?? null : null;
    const tp: Pt = vis ? { x: vis.x, z: vis.z } : (threat ?? { x: self.x, z: self.z });
```

（原来的 `player.x/z` 兜底改为自身位置——只有 threat 为空时才会走到，此时各模式都不会用 tp。）
- switch 之后、think 结尾加：`this.updateKnife(self);`
- 类内新增私有方法：

```ts
  // 切刀加速：没有任何威胁且目的地还远 → 亮刀赶路(速度=刀速)
  private updateKnife(self: Self): void {
    const d = this.drive;
    const threatened = this.senses.visible || !!this.senses.lastKnown || !!this.senses.heard;
    const goal = this.pathFor;
    d.knife = !threatened && !!goal && Math.hypot(goal.x - self.x, goal.z - self.z) > BRAIN.KNIFE_DIST
      && (d.mode === 'patrol' || d.mode === 'hunt' || d.mode === 'flank' || d.mode === 'investigate');
  }
```

注意：`hunt/investigate` 本身有 lastKnown/heard → threatened=true → 自动收刀，所以白名单里留着它们不影响行为（只是防未来新模式误亮刀）；`squad.shared` 不单独判——有 shared 时模式是 hunt/holdpos/flank，且这些模式的威胁来自 threat（lastKnown 为空时 threatened 仍 false，但 flank 赶远路亮刀合理，故规则保持简单）。

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test:fast` → Expected: 失败数 0；`npm run build` 会报 eggBots.ts 调 think 的参数错——**预期中**，看错误是否只有这一处；若只此一处，本任务先允许 build 红灯（Task 5 立即修复）。若想保绿：临时把 eggBots.ts 的 think 调用第三参改为 `playerAlive ? [{ id: 'player', x: playerPos.x, z: playerPos.z }] : []`（这是 Task 5 的正式写法，提前落地无妨），build 即绿。**采用后者**。

- [ ] **Step 5: 提交**

```bash
git add src/game/ai/botBrain.ts tests/botBrain.test.ts src/game/enemies/eggBots.ts
git commit -m "feat(爆破A): 大脑吃敌人列表(多目标)+切刀加速意图

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: eggBots 比赛模式——两队真枪互射

**Files:**
- Modify: `src/game/enemies/eggBots.ts`（大改）

**Interfaces:**
- Consumes: `Roster/Combatant/TeamId`（Task 1）、`TargetPing/visibleId`（Task 2）、`aimCfgForGun/AimCfg`（Task 3）、`Drive.knife`（Task 4）、`GunDef/GUN_BY_ID/dmgAt`（`../weapons/gunDefs`）、`createEgg(team)`（已支持 'blue'）
- Produces（Task 6/7/8 与计划B依赖，逐字匹配）:
  - `interface MatchBotSpec { id: string; name: string; team: TeamId }`
  - `interface MatchCfg { roster: Roster; blue: MatchBotSpec[]; red: MatchBotSpec[]; blueZone: Bounds; redZone: Bounds }`
  - `constructor(walls, bounds, count, spawnZone?, matchCfg?: MatchCfg)`（matchCfg 存在→match 模式；否则 practice 现状）
  - `setOnDeath(cb: (victimId: string, killerId: string, headshot: boolean) => void): void`
  - `setGun(botId: string, gun: GunDef): void`（换枪+同步 aim 节奏+roster.weaponId）
  - `assignRandomGuns(pool: GunDef[], rng?: () => number): void`（活蛋每只随机一把）
  - `resetForRound(): void`（match：全员复活满血回本方出生区、脑/瞄准/弹痕清空、roster 同步）
  - `teamView(team: TeamId): { x: number; z: number; yaw: number; alive: boolean }[]`（小地图用）
  - `hearGun(x, z, suppressed, shooterTeam: TeamId = 'blue')`、`hearFootstep(x, z, shooterTeam: TeamId = 'blue')`（只有**敌队**听见；practice 蛋全是 red、玩家默认 blue → 行为不变）
  - `shootObject(..., killerId = 'player')` 追加参数；match 模式打中**蓝队**返回 null（友军免伤，不留弹痕）
  - `debugState()` 每项增加 `{ id, name, team }`
  - match 模式规则：死亡不重生（隐藏至 resetForRound）、蛋速=knife?6.75:gun.runSpeed、伤害=dmgAt(gun,dist,false)、每次开枪向敌队 emitSound（半径按 suppressed）

- [ ] **Step 1: 通读现文件后按下述逐段修改**

（a）import 区追加：

```ts
import { Roster, TeamId } from '../combat/roster';
import { GunDef, GUN_BY_ID, dmgAt } from '../weapons/gunDefs';
import { aimCfgForGun } from '../ai/botAim';
```

（b）常量区 `const SPEED = 2.6;` 注释后补一行常量：

```ts
const KNIFE_SPEED = 6.75;   // 比赛模式:切刀跑图速度(同玩家)
```

（c）`interface Bot` 增加字段：

```ts
  id: string; name: string; team: TeamId;
  gun: GunDef;                // 手里的枪(practice也给标配,伤害走AIM.DMG不用它)
  knifeOut: boolean;          // 当前亮着刀(速度用刀速)
```

（d）类字段：删 `private squad = new BotSquad();`，改为：

```ts
  private squads: Record<TeamId, BotSquad> = { blue: new BotSquad(), red: new BotSquad() };
  private mode: 'practice' | 'match';
  private roster: Roster | null = null;
  private zones: Record<TeamId, Bounds> | null = null;
  private onDeath?: (victimId: string, killerId: string, headshot: boolean) => void;
```

（e）构造函数签名与蛋生成段替换：

```ts
  constructor(private walls: Box[], private bounds: Bounds, count: number, private spawnZone?: Bounds, matchCfg?: MatchCfg) {
    this.mode = matchCfg ? 'match' : 'practice';
    this.roster = matchCfg?.roster ?? null;
    this.zones = matchCfg ? { blue: matchCfg.blueZone, red: matchCfg.redZone } : null;
    // …(拖尾池/grid/covers/world 原样)…
    const specs: MatchBotSpec[] = matchCfg
      ? [...matchCfg.blue, ...matchCfg.red]
      : Array.from({ length: count }, (_, i) => ({ id: `red-${i}`, name: `蛋${i}`, team: 'red' as TeamId }));
    for (const spec of specs) {
      const p = this.spawnFor(spec.team);
      const egg = createEgg(spec.team);
      // …(缩放/位置/加组/材质 原样)…
      const brain = new BotBrain(); brain.reset();
      const aim = new BotAim(); aim.reset(p.x, p.z);
      const bot: Bot = {
        group: egg, bodyMat, brain, aim, thinkAcc: Math.random() * 0.1, bob: Math.random() * 6,
        stuck: 0, lx: p.x, lz: p.z, hp: MAX_HP, dead: false, respawn: 0, flash: 0, decals: [],
        id: spec.id, name: spec.name, team: spec.team, gun: GUN_BY_ID.classic, knifeOut: false,
      };
      this.bots.push(bot);
      this.roster?.add({ id: spec.id, name: spec.name, team: spec.team, x: p.x, z: p.z, hp: MAX_HP, alive: true, weaponId: 'classic', isPlayer: false });
    }
  }

  // 出生点：match按队伍区域,practice沿用spawnZone/全图
  private spawnFor(team: TeamId): { x: number; z: number } {
    if (this.mode === 'match' && this.zones) return this.clearPoint(this.zones[team]);
    return this.clearPoint(this.spawnZone);
  }
```

（`MatchBotSpec/MatchCfg` 两个 interface 放在文件顶部 `Bounds` 旁导出。）

（f）新公共方法（放 `setCombat` 附近）：

```ts
  setOnDeath(cb: (victimId: string, killerId: string, headshot: boolean) => void): void { this.onDeath = cb; }

  setGun(botId: string, gun: GunDef): void {
    const b = this.bots.find((x) => x.id === botId);
    if (!b) return;
    b.gun = gun;
    b.aim.setCfg(aimCfgForGun(gun.fireCd, gun.auto));
    this.roster?.setWeapon(botId, gun.id);
  }

  assignRandomGuns(pool: GunDef[], rng: () => number = Math.random): void {
    for (const b of this.bots) if (!b.dead) this.setGun(b.id, pool[Math.floor(rng() * pool.length)]);
  }

  // 回合重置：全员复活满血回本方出生区,脑/瞄准/弹痕清空(match用;下一回合开始时调)
  resetForRound(): void {
    for (const b of this.bots) {
      const sp = this.spawnFor(b.team);
      b.group.position.set(sp.x, 0, sp.z);
      b.lx = sp.x; b.lz = sp.z;
      b.hp = MAX_HP; b.dead = false; b.group.visible = true; b.respawn = 0;
      b.flash = 0; b.bodyMat.emissiveIntensity = 0;
      b.brain.reset(); b.aim.reset(sp.x, sp.z); b.thinkAcc = 0; b.knifeOut = false;
      this.clearDecals(b);
      this.roster?.setAlive(b.id, true); this.roster?.setHp(b.id, MAX_HP);
      this.roster?.updatePos(b.id, sp.x, sp.z, 0);
    }
  }

  teamView(team: TeamId): { x: number; z: number; yaw: number; alive: boolean }[] {
    return this.bots.filter((b) => b.team === team)
      .map((b) => ({ x: b.group.position.x, z: b.group.position.z, yaw: b.group.rotation.y, alive: !b.dead }));
  }
```

（g）声音改为"只有敌队听见"：

```ts
  hearGun(x: number, z: number, suppressed: boolean, shooterTeam: TeamId = 'blue'): void {
    this.emitSound(x, z, suppressed ? SENSE.NOISE_GUN_SUPPRESSED : SENSE.NOISE_GUN, shooterTeam);
  }
  hearFootstep(x: number, z: number, shooterTeam: TeamId = 'blue'): void { this.emitSound(x, z, SENSE.NOISE_FOOTSTEP, shooterTeam); }
  private emitSound(x: number, z: number, radius: number, shooterTeam: TeamId): void {
    if (!this.combat) return;
    for (const b of this.bots) {
      if (b.dead || b.team === shooterTeam) continue; // 自家枪声不当敌情
      const p = b.group.position;
      if (Math.hypot(p.x - x, p.z - z) <= radius) b.brain.senses.hearAt(x, z);
    }
  }
```

（h）`shootObject` 追加参数并加友军免伤（签名与首两行）：

```ts
  shootObject(obj: THREE.Object3D, hitPoint: THREE.Vector3, bodyDmg: number, headDmg: number, fromX: number, fromZ: number, killerId = 'player'): 'head' | 'body' | null {
    // …找 bot 原样…
    if (!bot || bot.dead) return null;
    if (this.mode === 'match' && bot.team === 'blue') return null; // 友军免伤(不掉血不留痕)
    const head = hitPoint.y > bot.group.position.y + HEAD_Y;
    this.damage(bot, head ? headDmg : bodyDmg, fromX, fromZ, killerId, head);
```

`tryMelee` 的候选循环里 `if (b.dead) continue;` 后加 `if (this.mode === 'match' && b.team === 'blue') continue;`，命中行改 `this.damage(best, KNIFE_DMG, this.tmpO.x, this.tmpO.z, 'player', false);`

（i）`damage` 签名与死亡段：

```ts
  private damage(b: Bot, dmg: number, fromX: number, fromZ: number, killerId: string, headshot: boolean): void {
    // …闪白/扣血/击退/pushOut/onDamaged 原样…
    this.roster?.setHp(b.id, Math.max(0, b.hp));
    if (b.hp <= 0) {
      b.dead = true;
      b.group.visible = false;
      b.respawn = this.mode === 'practice' ? RESPAWN_DELAY : Infinity; // 比赛不重生
      b.bodyMat.emissiveIntensity = 0;
      this.clearDecals(b);
      this.squads[b.team].noteDeath(); // 自家队友阵亡,本队谨慎
      this.roster?.setAlive(b.id, false);
      this.onDeath?.(b.id, killerId, headshot);
    }
  }
```

（j）`update()` 内改四处：
1. 小队更新（原 `const mates: SquadMate[] = ...; this.squad.update(dt, mates);`）替换为**分队**：

```ts
    const mates: Record<TeamId, SquadMate[]> = { blue: [], red: [] };
    const mateIdx: number[] = []; // bots[i] 在本队 mates 里的下标
    for (const b of this.bots) {
      const arr = mates[b.team];
      mateIdx.push(arr.length);
      arr.push({ alive: !b.dead, x: b.group.position.x, z: b.group.position.z, known: b.brain.senses.lastKnown, visible: b.brain.senses.visible });
    }
    this.squads.blue.update(dt, mates.blue);
    this.squads.red.update(dt, mates.red);
```

2. 思考调用（原 squadView 与 think 的两个实参）替换为：

```ts
        const squad = this.squads[b.team];
        const myMates = mates[b.team];
        const myIdx = mateIdx[i];
        const role = squad.roles[myIdx] ?? 'hold';
        const squadView = {
          role, shared: squad.shared, caution: squad.caution,
          flankCands: role === 'flank' ? squad.flankCandidates(myMates, myIdx) : [],
        };
        const targets = this.targetsFor(b.team, playerPos, playerAlive);
        b.brain.think(
          this.world,
          { x: p.x, z: p.z, faceX: Math.sin(b.group.rotation.y), faceZ: Math.cos(b.group.rotation.y), hp: b.hp },
          targets, squadView, b.thinkAcc, this.combat,
        );
```

3. 速度行 `const spd = SPEED * d.speedMul;` 替换：

```ts
      b.knifeOut = this.mode === 'match' && d.knife;
      const base = this.mode === 'match' ? (b.knifeOut ? KNIFE_SPEED : b.gun.runSpeed) : SPEED;
      const spd = base * d.speedMul;
```

4. 开火段整体替换（目标从 roster/玩家取、伤害按枪、双向命中、开枪出声）：

```ts
      const tgt = this.aimTargetFor(b, playerPos);
      const shot = tgt && b.aim.update(
        dt, d.weaponsFree && this.combat, b.brain.senses.visible,
        { x: p.x, z: p.z }, { x: tgt.x, z: tgt.z }, { x: tgt.vx, z: tgt.vz },
      );
      if (shot && tgt) {
        const wallPt = losClip(p.x, p.z, tgt.x, tgt.z, this.solids);
        if (wallPt) {
          this.spawnTracer(p.x, EGG_EYE + p.y, p.z, wallPt.x, 0.9 + Math.random() * 0.5, wallPt.z);
        } else {
          const ty = shot.hit ? tgt.eyeY - 0.15 : 0.7 + Math.random();
          this.spawnTracer(p.x, EGG_EYE + p.y, p.z, shot.x, ty, shot.z);
          if (shot.hit) {
            const dist = Math.hypot(tgt.x - p.x, tgt.z - p.z);
            const dmg = this.mode === 'match' ? dmgAt(b.gun, dist, false) : AIM.DMG;
            if (tgt.kind === 'player') { if (playerAlive && this.onHit) this.onHit(dmg, p.x, p.z); }
            else this.damage(tgt.bot, dmg, p.x, p.z, b.id, false);
          }
        }
        if (this.mode === 'match') this.hearGun(p.x, p.z, !!b.gun.suppressed, b.team); // 开枪暴露给敌队
      }
```

5. 类内新增两个私有辅助（放 update 前）：

```ts
  // 本队的敌人候选(比赛=名册敌队,训练=玩家)
  private targetsFor(team: TeamId, playerPos: THREE.Vector3, playerAlive: boolean): { id: string; x: number; z: number }[] {
    if (this.mode === 'match' && this.roster) {
      return this.roster.aliveEnemiesOf(team).map((c) => ({ id: c.id, x: c.x, z: c.z }));
    }
    return playerAlive ? [{ id: 'player', x: playerPos.x, z: playerPos.z }] : [];
  }

  // 当前瞄谁：可见目标的实时位置/速度(玩家来自参数与旧推导,蛋来自名册)
  private aimTargetFor(b: Bot, playerPos: THREE.Vector3):
    { kind: 'player'; x: number; z: number; vx: number; vz: number; eyeY: number }
    | { kind: 'bot'; bot: Bot; x: number; z: number; vx: number; vz: number; eyeY: number } | null {
    const id = b.brain.senses.visibleId;
    if (!id) return null;
    if (id === 'player') return { kind: 'player', x: playerPos.x, z: playerPos.z, vx: this.playerVel.x, vz: this.playerVel.z, eyeY: playerPos.y };
    const c = this.roster?.get(id);
    const tb = this.bots.find((x) => x.id === id);
    if (!c || !tb || tb.dead) return null;
    return { kind: 'bot', bot: tb, x: c.x, z: c.z, vx: c.vx, vz: c.vz, eyeY: EGG_EYE + tb.group.position.y };
  }
```

6. 每帧末尾（卡住检测之后、separation 之前的 per-bot 循环内）同步名册位置：

```ts
      this.roster?.updatePos(b.id, p.x, p.z, dt);
```

7. 死亡重生分支加保护（match 不重生）：`if (b.respawn <= 0)` 前保持 `b.respawn -= dt;`——match 的 respawn=Infinity 永不触发，无需其它改动。

（k）`debugState` 替换：

```ts
  debugState(): { id: string; name: string; team: TeamId; mode: string; role: string; hp: number; visible: boolean }[] {
    return this.bots.map((b) => ({
      id: b.id, name: b.name, team: b.team,
      mode: b.brain.drive.mode, role: 'hold', hp: b.hp, visible: b.brain.senses.visible,
    }));
  }
```

（role 精确回填需要队内索引，调试价值低，统一报 'hold'；正面名额断言改由计划B的导演测试保障——在报告里注明这一点。）

- [ ] **Step 2: 类型检查与测试**

Run: `npm run test:fast && npm run build` → Expected: 失败数 0、tsc 零错误（main.ts 旧调用兼容：新参数全部可选）

- [ ] **Step 3: 冒烟**

`npm run build && npm run preview` 起服务 curl 首页 200（无头替代冒烟；行为验证在 Task 8）。

- [ ] **Step 4: 提交**

```bash
git add src/game/enemies/eggBots.ts
git commit -m "feat(爆破A): 蛋蛋两队化——真枪互射/友军免伤/死亡事件/切刀提速/回合重置API

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: main.ts 混战模式 + 训练场按钮

**Files:**
- Modify: `src/main.ts`、`index.html`

**Interfaces:**
- Consumes: Task 1/5 全部产出
- Produces: 全局 `gameMode: 'skirmish' | 'practice'`；DEV 钩子 `__roster`、`__mode()`；`#btn-practice` 按钮；混战获胜横幅 `#skirmish-banner`

- [ ] **Step 1: index.html 两处**

`<button id="btn-start" ...>开始游戏</button>` 文案改为 `5v5 混战`；紧挨其后加同款主按钮：

```html
        <button id="btn-practice" class="btn btn-primary">训练场</button>
```

HUD 区（`id="freeze"` 元素旁）加横幅节点：

```html
    <div id="skirmish-banner" class="hidden" style="position:fixed;top:38%;left:50%;transform:translateX(-50%);font:bold 42px system-ui;color:#ffd35a;text-shadow:0 2px 8px #000;z-index:30;pointer-events:none"></div>
```

- [ ] **Step 2: main.ts 接线**（按锚点逐处改）

（a）import 区加：

```ts
import { Roster } from './game/combat/roster';
```

（b）`new EggBots(...)`（约 :73）整段替换为双实例 + 名册：

```ts
const roster = new Roster();
roster.add({ id: 'player', name: '你', team: 'blue', x: map.attackerSpawn.x, z: map.attackerSpawn.z, hp: 100, alive: true, weaponId: 'classic', isPlayer: true });
const botBounds = { minX: bMinX + 3, maxX: bMaxX - 3, minZ: bMinZ + 3, maxZ: bMaxZ - 3 };
const zoneAround = (p: { x: number; z: number }, r: number) => ({ minX: p.x - r, maxX: p.x + r, minZ: p.z - r, maxZ: p.z + r });
// 训练场：老样子 6 只红蛋(出生限守方半场)
// ⚠️ 原 new EggBots 调用若已带 spawnZone 表达式,practice 这里照抄原值,别用下面的近似
const eggBotsPractice = new EggBots(map.walls, botBounds, 6, zoneAround(map.defenderSpawn, 14));
// 5v5 混战：蓝4(你的队友)+红5
const eggBotsMatch = new EggBots(map.walls, botBounds, 0, undefined, {
  roster,
  blue: ['蛋定', '蛋壳', '蛋黄', '蛋花'].map((n, i) => ({ id: `blue-${i}`, name: n, team: 'blue' as const })),
  red: ['皮蛋', '咸蛋', '铁蛋', '坏蛋', '笨蛋'].map((n, i) => ({ id: `red-${i}`, name: n, team: 'red' as const })),
  blueZone: zoneAround(map.attackerSpawn, 10),
  redZone: zoneAround(map.defenderSpawn, 10),
});
let gameMode: 'skirmish' | 'practice' = 'skirmish';
const eggBots = { // 轻代理:游戏其余代码照旧用 eggBots.xxx
  get active(): EggBots { return gameMode === 'practice' ? eggBotsPractice : eggBotsMatch; },
};
```

随后**全文**把原先对 `eggBots.` 的调用改为 `eggBots.active.`（约 10 处：knife.onStrike、DEV 钩子、raiseBarriers×2、dropBarriers×2、setOnHit、fireGunShot 的 shootObject、startGame/backToMenu 的 scene.add/remove、主循环 update、hearGun/hearFootstep）。`setOnHit` 要对**两个实例**都设置：

```ts
eggBotsPractice.setOnHit((dmg) => damagePlayer(dmg));
eggBotsMatch.setOnHit((dmg) => damagePlayer(dmg));
```

（c）击杀事件（放 setOnHit 旁）：

```ts
// 混战:记录击杀(计划B会接击杀反馈UI);DEV可查
const killLog: { victim: string; killer: string; headshot: boolean }[] = [];
eggBotsMatch.setOnDeath((victimId, killerId, headshot) => { killLog.push({ victim: victimId, killer: killerId, headshot }); });
if (import.meta.env.DEV) (window as unknown as { __kills: typeof killLog }).__kills = killLog;
```

（d）`startGame()` 改签名 `function startGame(mode: 'skirmish' | 'practice' = 'skirmish'): void`，函数体开头 `state = 'play';` 前加：

```ts
  gameMode = mode;
```

`scene.add(eggBots.active.group)`（原句已改代理）之后加混战初始化：

```ts
  if (gameMode === 'skirmish') { eggBotsMatch.resetForRound(); eggBotsMatch.assignRandomGuns(GUNS); roster.setAlive('player', true); roster.setHp('player', PLAYER_MAX_HP); }
```

按钮监听（约 :511）改/加：

```ts
document.getElementById('btn-start')?.addEventListener('click', () => startGame('skirmish'));
document.getElementById('btn-practice')?.addEventListener('click', () => startGame('practice'));
```

（e）玩家与名册同步 + 混战裁判（主循环 `eggBots.active.update(dt, camera.position, !playerDead);` 之后加）：

```ts
    if (gameMode === 'skirmish') {
      roster.updatePos('player', camera.position.x, camera.position.z, dt);
      const bannerEl = document.getElementById('skirmish-banner');
      if (wipeCd > 0) { // 横幅期:倒计时后重开一局
        wipeCd -= dt;
        if (wipeCd <= 0) {
          bannerEl?.classList.add('hidden');
          eggBotsMatch.resetForRound(); eggBotsMatch.assignRandomGuns(GUNS);
          if (playerDead) respawnPlayer(); else { playerHp = PLAYER_MAX_HP; refreshHpHud(); }
          player.teleport(map.attackerSpawn);
          roster.setAlive('player', true); roster.setHp('player', PLAYER_MAX_HP);
          raiseBarriers();
        }
      } else {
        const blueAlive = roster.aliveCount('blue'), redAlive = roster.aliveCount('red');
        if (!barriersUp && (blueAlive === 0 || redAlive === 0)) {
          wipeCd = 4;
          if (bannerEl) { bannerEl.textContent = blueAlive === 0 ? '红队获胜！' : '蓝队获胜！'; bannerEl.classList.remove('hidden'); }
        }
      }
    }
```

变量声明区（`let stepT = 0;` 旁）加：`let wipeCd = 0; // 混战:一队全灭后横幅倒计时`
`damagePlayer` 内 `playerDead = true` 的语句块里加一行 `roster.setAlive('player', false);`；`respawnPlayer()` 里加 `roster.setAlive('player', true); roster.setHp('player', PLAYER_MAX_HP);`
`startGame` 里 `raiseBarriers();` 保持原位（每局都有 15 秒买枪准备）。`backToMenu` 里加 `wipeCd = 0; document.getElementById('skirmish-banner')?.classList.add('hidden');`

（f）DEV 钩子区加：

```ts
  (window as unknown as { __roster: Roster }).__roster = roster;
  (window as unknown as { __mode: () => string }).__mode = () => gameMode;
```

- [ ] **Step 3: 测试+类型+冒烟**

Run: `npm run test:fast && npm run build` → 失败数 0、零类型错误。

- [ ] **Step 4: 提交**

```bash
git add src/main.ts index.html
git commit -m "feat(爆破A): 5v5混战上线——两队开打/全灭横幅重开/训练场独立按钮

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 小地图队友图标

**Files:**
- Modify: `src/game/ui/minimap.ts`、`src/main.ts`（draw 调用处）

**Interfaces:**
- Consumes: `eggBots.active.teamView('blue')`（Task 5）
- Produces: `Minimap.draw(px, pz, yaw, showBarriers = true, mates: { x: number; z: number; yaw: number; alive: boolean }[] = [])`

- [ ] **Step 1: minimap.ts** —— `draw` 签名加第 5 参 `mates: { x: number; z: number; yaw: number; alive: boolean }[] = []`；在"玩家朝向箭头"段**之前**插入：

```ts
    // 队友：蓝点+朝向短线；阵亡=灰色小叉
    for (const m of mates) {
      const mx = mapX(m.x), my = mapY(m.z);
      if (m.alive) {
        ctx.save();
        ctx.translate(mx, my);
        ctx.rotate(-m.yaw);
        ctx.strokeStyle = '#8fd0ff'; ctx.lineWidth = 2;
        ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(0, -6); ctx.stroke(); // 朝向线
        ctx.fillStyle = '#4aa3ff'; ctx.strokeStyle = '#0b2a4a'; ctx.lineWidth = 1;
        ctx.beginPath(); ctx.arc(0, 0, 3.5, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
        ctx.restore();
      } else {
        ctx.strokeStyle = 'rgba(160,160,160,0.9)'; ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(mx - 4, my - 4); ctx.lineTo(mx + 4, my + 4);
        ctx.moveTo(mx + 4, my - 4); ctx.lineTo(mx - 4, my + 4);
        ctx.stroke();
      }
    }
```

- [ ] **Step 2: main.ts 调用处**（主循环 `minimap?.draw(...)`）改为：

```ts
      minimap?.draw(camera.position.x, camera.position.z, camera.rotation.y, barriersUp,
        gameMode === 'skirmish' ? eggBots.active.teamView('blue') : []);
```

- [ ] **Step 3: 测试+类型**

Run: `npm run test:fast && npm run build` → 失败数 0、零错误。

- [ ] **Step 4: 提交**

```bash
git add src/game/ui/minimap.ts src/main.ts
git commit -m "feat(爆破A): 小地图队友图标——蓝点带朝向/阵亡灰叉

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: 无头验证 5v5 混战

**Files:**
- Create: 验证脚本放 scratchpad（不进仓库）

**Interfaces:**
- Consumes: DEV 钩子 `__wp.dropBarriers()/state()`、`__roster`、`__kills`、`__eggBots`(=当前实例可用 `__mode()` 判断)、`#btn-start`

- [ ] **Step 1: 环境**（照既有经验）：`npm run dev` 后台起服；playwright 用 `executablePath: '/root/.cache/ms-playwright/chromium-1223/chrome-linux/chrome'`；scratchpad 里 `npm i playwright@1.60`（PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1）；**游戏内时间等待**（无头 fps 低）。

- [ ] **Step 2: 断言**（脚本要点）

1. 点 `#btn-start` → `__wp.dropBarriers()`；确认 `__mode()==='skirmish'`。
2. **互射发生**：45 游戏秒内 `__kills.length ≥ 2` 且既有蓝名也有红名的 victim（蛋名判断队伍：victim 以 `blue-`/`red-` 开头）。
3. **玩家也在战场**：把玩家传送到红方半场附近静立（`__player.teleport`），确认 `__wp.state().hp < 100` 在 30 游戏秒内发生（红蛋会打你），且蓝蛋从不伤你（记录 onHit 来源无法直接看——以 hp 只在红蛋可见时下降为软证据，主要靠代码审查+友军免伤单测在 Task 5 前提下的 shootObject 行为）。
4. **全灭重开**：等到某队 aliveCount=0 → `#skirmish-banner` 可见，≈4 秒后 `barriersUp===true`（重开新局）且双方 alive 恢复 5/5（含玩家）。
5. **切刀加速可观察**：买枪阶段(光幕未落,无威胁)采样某蓝蛋 2 秒位移速度 >4 m/s（刀速 6.75 的证据；容差取 >4 抗低帧）。
6. 控制台零报错；截图一张混战现场。

- [ ] **Step 3: 结果处置** —— 全过：把统计写进报告；失败：按 debugState/console 诊断根因报告（不改数值不越权修）。

- [ ] **Step 4: 无提交**（脚本不进库；报告归档 .superpowers/sdd/）

---

## 计划自查记录

- **规格覆盖（对照爆破设计单§计划A范围）**：roster→T1；多目标感知→T2；真枪节奏→T3；切刀意图→T4；两队/伤害/事件/重置/免伤/提速→T5；混战+训练场入口→T6；小地图队友→T7；验证→T8。爆蛋器/回合/经济/观战/击杀UI/播报/匹配=计划B（设计单施工顺序注明）。
- **占位符**：无 TBD；所有代码可照抄。
- **类型一致性**：`TargetPing{id,x,z}` T2↔T4↔T5；`AimCfg/aimCfgForGun` T3↔T5；`Drive.knife` T4↔T5；`MatchCfg/teamView/setOnDeath/resetForRound` T5↔T6/T7/T8；`Roster` 方法名 T1↔T5/T6 逐字核对无误。
- **已知妥协**（报告与终审可见）：debugState 的 role 统一报 'hold'（分队后索引换算价值低）；蛋不会上高台（2D AI 既有限制）；Task 4 若 build 短暂红灯按 Step 4 的"提前落地写法"保绿。
