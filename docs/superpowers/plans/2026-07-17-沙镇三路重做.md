# 沙镇三路大改造 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把默认地图换成自创的"三路双包点"经典爆破布局（A长道/中门/B隧道），保留沙漠+晴天画风。

**Architecture:** 地图=数据（`mapGrid.ts` 的 ASCII 图纸 → `gridToObjects` → `buildDesertMap` 渲染）。本次只换图纸+扩图例，渲染/寻路/小地图全部自适应。

**Tech Stack:** TypeScript + Three.js；测试走 `npm run test:fast`（esbuild+vitest shim）。

## Global Constraints

- 不复刻 CS:GO 官方地图：布局为自创三路结构（设计单：docs/superpowers/specs/2026-07-17-沙镇三路重做-design.md）。
- 图例新增字母必须向后兼容（老图纸照常解析）。
- `MAP_KEY` 升 v3（旧 localStorage 存档不得顶掉新图）。
- 完成后必须无头截图验证 + push + `npm run deploy`（用户只看线上版）。

---

### Task 1: 图例扩展（C=警家出生点、= 光幕带朝向）

**Files:**
- Modify: `src/game/world/mapData.ts:55-68`（gridToObjects）
- Test: `tests/mapData.test.ts`（新建）
- Modify: `scripts/run-logic-tests.ts`（若测试靠显式 import 则注册新文件）

**Interfaces:**
- Produces: `gridToObjects(grid: string): MapObj[]` 新识别 `'Cc警'`→spawnC、`'='`→barrier；barrier 的 `ry`：左右邻格是 `#/▩/=` → 0（横跨南北走廊），否则上下邻格是 → `Math.PI/2`，默认 0。

- [ ] **Step 1: 写失败测试**

```ts
// tests/mapData.test.ts
import { describe, it, expect } from 'vitest';
import { gridToObjects } from '../src/game/world/mapData';

describe('图例扩展', () => {
  it('C 解析成警家出生点', () => {
    const objs = gridToObjects(`\n###\n#C#\n###\n`);
    expect(objs.some((o) => o.t === 'spawnC')).toBe(true);
  });
  it('= 左右贴墙 → 横跨走廊(ry=0)', () => {
    const objs = gridToObjects(`\n###\n#=#\n###\n`);
    const b = objs.find((o) => o.t === 'barrier');
    expect(b?.ry).toBe(0);
  });
  it('= 上下贴墙 → 竖跨门洞(ry=π/2)', () => {
    const objs = gridToObjects(`\n.#.\n.=.\n.#.\n`);
    const b = objs.find((o) => o.t === 'barrier');
    expect(b?.ry).toBeCloseTo(Math.PI / 2);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**：`npm run test:fast` → mapData 三项 FAIL。
- [ ] **Step 3: 实现**（gridToObjects 内）：

```ts
const at = (rr: number, cc: number): string => rows[rr]?.[cc] ?? ' ';
const solidish = (s: string): boolean => '#▩='.includes(s);
// 循环里追加分支：
else if ('Cc警'.includes(ch)) objs.push(makeObj('spawnC', x, z));
else if (ch === '=') {
  const horiz = solidish(at(r, c - 1)) || solidish(at(r, c + 1));
  objs.push(makeObj('barrier', x, z, horiz ? 0 : Math.PI / 2));
}
```

- [ ] **Step 4: 跑测试确认通过**：`npm run test:fast` 全绿。
- [ ] **Step 5: Commit**：`feat(图纸): 图例支持警家出生点C和光幕=(自动判朝向)`

### Task 2: 新三路图纸 + MAP_KEY v3 + 连通性测试

**Files:**
- Modify: `src/game/world/mapGrid.ts`（GRID 整体替换，图例注释补 C/=）
- Modify: `src/game/world/mapData.ts:15`（`MAP_KEY` → `'cmgo_map_v3'`）
- Test: `tests/mapData.test.ts` 追加

**Interfaces:**
- Consumes: Task 1 的图例。
- Produces: 新 `GRID`（24 列 × 22 行，见下）。

- [ ] **Step 1: 追加失败测试（连通性 + 结构）**

```ts
import { GRID } from '../src/game/world/mapGrid';

describe('三路图纸', () => {
  const rows = GRID.split('\n').map((r) => r.replace(/\s+$/, '')).filter((r) => r.length > 0);
  const walk = (ch: string): boolean => !'#▩HhX'.includes(ch); // 连通不许靠跳箱子
  const reach = (from: string, to: string): boolean => {
    const seen = new Set<string>(); const q: [number, number][] = [];
    rows.forEach((row, r) => [...row].forEach((c2, c) => { if (c2 === from) q.push([r, c]); }));
    while (q.length) {
      const [r, c] = q.pop() as [number, number];
      const k = r + ',' + c; if (seen.has(k)) continue; seen.add(k);
      const ch = rows[r]?.[c] ?? '#';
      if (ch === to) return true;
      if (!walk(ch)) continue;
      q.push([r + 1, c], [r - 1, c], [r, c + 1], [r, c - 1]);
    }
    return false;
  };
  it('匪家能到警家/A/B', () => {
    expect(reach('S', 'C')).toBe(true);
    expect(reach('S', 'A')).toBe(true);
    expect(reach('S', 'B')).toBe(true);
  });
  it('外圈城墙封死', () => {
    expect([...rows[0]].every((c) => c === '#')).toBe(true);
    expect([...rows[rows.length - 1]].every((c) => c === '#')).toBe(true);
    rows.forEach((r) => { expect(r[0]).toBe('#'); expect(r[r.length - 1]).toBe('#'); });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**（老图没有 C，第一项 FAIL）。
- [ ] **Step 3: 换图纸**（`mapGrid.ts`，图例注释加 `C=警家出生点`、`==光幕`）：

```ts
export const GRID = `
########################
#......#...CC...#......#
#.BB.X.=........=.XAA..#
#.BB...####==####..AAX.#
#..X...................#
#......##......###..####
##..#####......#H#..#HH#
##..#####X....X#H#..#HH#
##..#####......#H#..#HH#
##..#######..#####..####
##.....##......###..####
##.....##......###X.####
#####..####..#####..####
#HH##..##......#H#..#HH#
#HH##..##.X....#H#..#HH#
#####..##....X.###..####
#####==##======###==####
##....................##
########........########
########...SS...########
########........########
########################
`;
```

布局对照：西=B隧道(S形两拐角)、中=中庭+双门+南中路、东=A长道(60米直线,c18r11探头箱)；北带r4=防守转点环廊、南带r17=进攻前场；`=`共11格=三条路口+警家三门的光幕。

- [ ] **Step 4: MAP_KEY 改 v3**；跑 `npm run test:fast` 全绿 + `npx tsc --noEmit`。
- [ ] **Step 5: Commit**：`feat(地图): 三路双包点新沙镇图纸 + 存档键v3`

### Task 3: 版本角标 + 无头视觉验证

**Files:**
- Modify: `index.html:234`（`v0.3 · 沙漠小镇` → `v0.4 · 三路沙镇`）
- 脚本: 复用 scratchpad `verify-sunny.mjs` 思路，新写 `verify-map.mjs`

**Interfaces:** Consumes: 全部前序任务。

- [ ] **Step 1:** 改角标文案。
- [ ] **Step 2:** 无头截图：主菜单（展台跟随匪家出生点公式自动挪，构图不好看就在 `src/main.ts:61` 手调坐标）、传送到 A长道南口/中门/B隧道拐角/两包点各截一张（用 `__player.teleport` + 设 `__player.yaw`）。
- [ ] **Step 3:** 人眼检查截图：无破洞、视线符合设计、帧率面板正常（无头 12-20fps 属正常，对照上次同环境数值）。
- [ ] **Step 4: Commit**：`feat(地图): 三路沙镇上线 + 角标v0.4`（含截图确认说明）。

### Task 4: 发布上线

- [ ] **Step 1:** `git push origin main`。
- [ ] **Step 2:** `npm run deploy` → 输出 `Published`。
- [ ] **Step 3:** 轮询线上 `curl https://nornttyy.github.io/CMGO/ | grep "v0.4"`（约1分钟生效）。
- [ ] **Step 4:** 告知用户 Ctrl+F5 强刷试玩。

## Self-Review 结论

- 设计单五条验收标准 ↔ Task2(连通性/测试)、Task3(截图/帧率)、Task4(上线) 全覆盖。
- 图纸 22 行每行 24 字符已逐行核对；光幕 11 格朝向经 Task1 启发式全部为"左右贴墙→ry=0"，警家两侧门 `=`(r2c7/r2c16) 上下贴墙→π/2 ✓。
- 无 TBD/占位；类型签名与现有 `MapObj`/`makeObj(t,x,z,ry)` 一致（makeObj 第4参已存在，默认0）。
